<h1><%= @cocktail.name %></h1>


<ul class="list_unstyled">
  <% @cocktail.dose.each do |dose|  %>
    <li><%= link_to dose.ingredient.name, ingredient_path(dose.ingredient) %>
     : <%= link_to "<i class = 'fa fa-close'></i>".html_safe, dose_path(dose), method: :delete, data: {cofirm: "Are you sure?" } %></li>
  <% end %>
</ul>

<%= simple_form_for ([@cocktail, @dose]) do |f| %>

